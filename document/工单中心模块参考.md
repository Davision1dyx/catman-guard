<!-- 代码已包含 CSS：使用 TailwindCSS , 安装 TailwindCSS 后方可看到布局样式效果 -->

<template>
  <div class="min-h-screen bg-gray-50 flex">
    <!-- 侧边导航栏 -->
    <div class="w-64 bg-white shadow-lg h-screen fixed left-0 top-0 overflow-y-auto">
      <!-- Logo 区 -->
      <div class="p-6 border-b border-gray-200">
        <div class="flex items-center space-x-3">
          <div class="bg-blue-600 p-2 rounded-lg">
            <i class="fas fa-brain text-white text-xl"></i>
          </div>
          <h1 class="text-xl font-bold text-gray-800">智维 Agent</h1>
        </div>
      </div>

      <!-- 核心菜单 -->
      <nav class="p-4">
        <ul class="space-y-2">
          <li>
            <a href="#" class="flex items-center p-3 rounded-lg hover:bg-gray-100 text-gray-700">
              <i class="fas fa-comments mr-3"></i>
              <span>智能问答</span>
            </a>
          </li>
          <li>
            <a href="#" class="flex items-center p-3 rounded-lg hover:bg-gray-100 text-gray-700">
              <i class="fas fa-book mr-3"></i>
              <span>知识库管理</span>
            </a>
          </li>
          <li>
            <a href="#" class="flex items-center p-3 rounded-lg bg-blue-50 text-blue-600">
              <i class="fas fa-tasks mr-3"></i>
              <span>工单中心</span>
            </a>
          </li>
          <li>
            <a href="#" class="flex items-center p-3 rounded-lg hover:bg-gray-100 text-gray-700">
              <i class="fas fa-chart-bar mr-3"></i>
              <span>数据看板</span>
            </a>
          </li>
          <li>
            <a href="#" class="flex items-center p-3 rounded-lg hover:bg-gray-100 text-gray-700">
              <i class="fas fa-calendar-alt mr-3"></i>
              <span>值班排班</span>
            </a>
          </li>
          <li>
            <a href="#" class="flex items-center p-3 rounded-lg hover:bg-gray-100 text-gray-700">
              <i class="fas fa-cog mr-3"></i>
              <span>系统设置</span>
            </a>
          </li>
        </ul>
      </nav>

      <!-- 用户信息区 -->
      <div class="absolute bottom-0 w-full p-4 border-t border-gray-200">
        <div class="flex items-center space-x-3">
          <div class="w-10 h-10 rounded-full bg-blue-100 flex items-center justify-center">
            <i class="fas fa-user text-blue-600"></i>
          </div>
          <div>
            <p class="font-medium text-gray-800">张伟</p>
            <p class="text-xs text-gray-500">值班人员</p>
          </div>
          <div class="ml-auto">
            <div class="w-3 h-3 rounded-full bg-green-500"></div>
          </div>
        </div>
      </div>
    </div>

    <!-- 主内容区 -->
    <div class="ml-64 flex-1 min-h-screen">
      <!-- 顶部状态栏 -->
      <header class="bg-white shadow-sm p-4">
        <div class="flex items-center justify-between">
          <div class="flex items-center space-x-4">
            <nav class="flex" aria-label="Breadcrumb">
              <ol class="inline-flex items-center space-x-1 md:space-x-3">
                <li class="inline-flex items-center">
                  <i class="fas fa-home text-gray-400 mr-1"></i>
                  <span class="text-gray-700">首页</span>
                </li>
                <li>
                  <div class="flex items-center">
                    <i class="fas fa-chevron-right text-gray-400 mx-2 text-xs"></i>
                    <span class="text-gray-700">工单中心</span>
                  </div>
                </li>
              </ol>
            </nav>
          </div>
          <div class="flex items-center space-x-4">
            <!-- 全局搜索 -->
            <div class="relative">
              <input
                v-model="searchQuery"
                type="text"
                placeholder="搜索工单、标题或内容..."
                class="pl-10 pr-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent text-sm"
              />
              <i class="fas fa-search absolute left-3 top-3 text-gray-400"></i>
            </div>
            <!-- 通知中心 -->
            <button class="relative p-2 text-gray-500 hover:text-gray-700">
              <i class="fas fa-bell"></i>
              <span class="absolute top-0 right-0 w-4 h-4 bg-red-500 text-white text-xs rounded-full flex items-center justify-center">3</span>
            </button>
          </div>
        </div>
      </header>

      <!-- 主工作区 -->
      <main class="p-6">
        <!-- 页面头部 -->
        <div class="mb-8">
          <div class="flex justify-between items-center">
            <h1 class="text-2xl font-bold text-gray-800">工单中心</h1>
            <div class="flex space-x-3">
              <button 
                @click="createNewTicket"
                class="!rounded-button whitespace-nowrap px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 flex items-center"
              >
                <i class="fas fa-plus mr-2"></i>
                新建工单
              </button>
            </div>
          </div>
          <p class="text-gray-600 mt-2">管理系统中的所有工单，跟踪处理进度和状态</p>
        </div>

        <!-- 工单筛选器 -->
        <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6 mb-8">
          <div class="grid grid-cols-1 md:grid-cols-4 gap-4">
            <div>
              <label class="block text-sm font-medium text-gray-700 mb-2">工单状态</label>
              <select 
                v-model="filterStatus"
                class="w-full p-2 border border-gray-300 rounded-lg text-sm"
              >
                <option value="">全部状态</option>
                <option value="ASSIGNED">已派单</option>
                <option value="IN_PROGRESS">处理中</option>
                <option value="COMPLETED">完成</option>
                <option value="VECTOR_STORED">向量存储</option>
              </select>
            </div>
            <div>
              <label class="block text-sm font-medium text-gray-700 mb-2">优先级</label>
              <select 
                v-model="filterPriority"
                class="w-full p-2 border border-gray-300 rounded-lg text-sm"
              >
                <option value="">全部优先级</option>
                <option value="LOW">低</option>
                <option value="MEDIUM">中</option>
                <option value="HIGH">高</option>
                <option value="CRITICAL">紧急</option>
              </select>
            </div>
            <div>
              <label class="block text-sm font-medium text-gray-700 mb-2">负责人</label>
              <select 
                v-model="filterAssignee"
                class="w-full p-2 border border-gray-300 rounded-lg text-sm"
              >
                <option value="">全部负责人</option>
                <option value="张伟">张伟</option>
                <option value="李娜">李娜</option>
                <option value="王强">王强</option>
                <option value="陈丽">陈丽</option>
              </select>
            </div>
            <div>
              <label class="block text-sm font-medium text-gray-700 mb-2">工单类型</label>
              <select 
                v-model="filterType"
                class="w-full p-2 border border-gray-300 rounded-lg text-sm"
              >
                <option value="">全部类型</option>
                <option value="INCIDENT">故障报告</option>
                <option value="REQUEST">服务请求</option>
                <option value="PROBLEM">问题分析</option>
                <option value="CHANGE">变更请求</option>
              </select>
            </div>
          </div>
        </div>

        <!-- 工单列表 -->
        <div class="bg-white rounded-lg shadow-sm border border-gray-200 overflow-hidden">
          <div class="p-6 border-b border-gray-200">
            <h2 class="text-lg font-semibold text-gray-800">工单列表</h2>
          </div>
          
          <div class="overflow-x-auto">
            <table class="min-w-full divide-y divide-gray-200">
              <thead class="bg-gray-50">
                <tr>
                  <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">工单编号</th>
                  <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">标题</th>
                  <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">类型</th>
                  <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">优先级</th>
                  <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">状态</th>
                  <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">提交人</th>
                  <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">负责人</th>
                  <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">创建时间</th>
                  <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">操作</th>
                </tr>
              </thead>
              <tbody class="bg-white divide-y divide-gray-200">
                <tr v-for="ticket in filteredTickets" :key="ticket.id" class="hover:bg-gray-50">
                  <td class="px-6 py-4 whitespace-nowrap">
                    <div class="text-sm font-medium text-blue-600">#{{ ticket.id }}</div>
                  </td>
                  <td class="px-6 py-4">
                    <div class="text-sm font-medium text-gray-900">{{ ticket.title }}</div>
                    <div class="text-sm text-gray-500 line-clamp-1">{{ ticket.description }}</div>
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap">
                    <span class="px-2 py-1 text-xs rounded-full" :class="getTypeClass(ticket.type)">
                      {{ getTypeText(ticket.type) }}
                    </span>
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap">
                    <span class="px-2 py-1 text-xs rounded-full" :class="getPriorityClass(ticket.priority)">
                      {{ getPriorityText(ticket.priority) }}
                    </span>
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap">
                    <span class="px-2 py-1 text-xs rounded-full" :class="getStatusClass(ticket.status)">
                      {{ getStatusText(ticket.status) }}
                    </span>
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">{{ ticket.submitter.name }}</td>
                  <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">{{ ticket.assignee.name || '-' }}</td>
                  <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">{{ formatDate(ticket.createdAt) }}</td>
                  <td class="px-6 py-4 whitespace-nowrap text-sm">
                    <div class="flex space-x-2">
                      <button 
                        @click="viewTicket(ticket)"
                        class="text-blue-600 hover:text-blue-900"
                        title="查看详情"
                      >
                        <i class="fas fa-eye"></i>
                      </button>
                      <button 
                        @click="editTicket(ticket)"
                        class="text-green-600 hover:text-green-900"
                        :title="canEditTicket(ticket) ? '编辑工单' : '无编辑权限'"
                        :disabled="!canEditTicket(ticket)"
                      >
                        <i class="fas fa-edit"></i>
                      </button>
                      <button 
                        @click="assignTicket(ticket)"
                        class="text-purple-600 hover:text-purple-900"
                        :title="canAssignTicket(ticket) ? '分配工单' : '无分配权限'"
                        :disabled="!canAssignTicket(ticket)"
                      >
                        <i class="fas fa-user-check"></i>
                      </button>
                      <button 
                        @click="updateTicketStatus(ticket)"
                        class="text-yellow-600 hover:text-yellow-900"
                        :title="canUpdateStatus(ticket) ? '更新状态' : '无更新权限'"
                        :disabled="!canUpdateStatus(ticket)"
                      >
                        <i class="fas fa-sync-alt"></i>
                      </button>
                    </div>
                  </td>
                </tr>
              </tbody>
            </table>
          </div>
          
          <div v-if="filteredTickets.length === 0" class="p-12 text-center">
            <i class="fas fa-inbox text-gray-300 text-4xl mb-4"></i>
            <p class="text-gray-500">暂无符合条件的工单</p>
          </div>
        </div>
      </main>

      <!-- 工单详情模态框 -->
      <div v-if="showTicketDetailModal" class="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50">
        <div class="bg-white rounded-lg shadow-xl w-4/5 max-w-4xl max-h-[90vh] overflow-y-auto">
          <div class="p-6 border-b border-gray-200 flex justify-between items-center">
            <h3 class="text-lg font-semibold text-gray-800">工单详情 - #{{ currentTicket?.id }}</h3>
            <button @click="closeTicketDetailModal" class="text-gray-500 hover:text-gray-700">
              <i class="fas fa-times"></i>
            </button>
          </div>
          <div class="p-6">
            <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-6">
              <div>
                <h4 class="text-sm font-medium text-gray-500">工单编号</h4>
                <p class="text-lg font-semibold text-gray-900">#{{ currentTicket?.id }}</p>
              </div>
              <div>
                <h4 class="text-sm font-medium text-gray-500">状态</h4>
                <p class="text-lg font-semibold text-gray-900">
                  <span class="px-2 py-1 text-sm rounded-full" :class="getStatusClass(currentTicket?.status || '')">
                    {{ getStatusText(currentTicket?.status || '') }}
                  </span>
                </p>
              </div>
              <div>
                <h4 class="text-sm font-medium text-gray-500">优先级</h4>
                <p class="text-lg font-semibold text-gray-900">
                  <span class="px-2 py-1 text-sm rounded-full" :class="getPriorityClass(currentTicket?.priority || '')">
                    {{ getPriorityText(currentTicket?.priority || '') }}
                  </span>
                </p>
              </div>
            </div>

            <div class="mb-6">
              <h4 class="text-sm font-medium text-gray-500 mb-2">标题</h4>
              <p class="text-lg font-semibold text-gray-900">{{ currentTicket?.title }}</p>
            </div>

            <div class="mb-6">
              <h4 class="text-sm font-medium text-gray-500 mb-2">类型</h4>
              <span class="px-2 py-1 text-sm rounded-full" :class="getTypeClass(currentTicket?.type || '')">
                {{ getTypeText(currentTicket?.type || '') }}
              </span>
            </div>

            <div class="mb-6">
              <h4 class="text-sm font-medium text-gray-500 mb-2">描述</h4>
              <div class="bg-gray-50 p-4 rounded-lg border">
                <p class="text-gray-700 whitespace-pre-line">{{ currentTicket?.description }}</p>
              </div>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-6">
              <div>
                <h4 class="text-sm font-medium text-gray-500 mb-2">提交人</h4>
                <div class="flex items-center">
                  <div class="w-8 h-8 rounded-full bg-blue-100 flex items-center justify-center mr-3">
                    <i class="fas fa-user text-blue-600"></i>
                  </div>
                  <div>
                    <p class="font-medium text-gray-900">{{ currentTicket?.submitter.name }}</p>
                    <p class="text-sm text-gray-500">{{ currentTicket?.submitter.email }}</p>
                  </div>
                </div>
              </div>
              <div>
                <h4 class="text-sm font-medium text-gray-500 mb-2">负责人</h4>
                <div class="flex items-center">
                  <div class="w-8 h-8 rounded-full bg-green-100 flex items-center justify-center mr-3">
                    <i class="fas fa-user-check text-green-600"></i>
                  </div>
                  <div>
                    <p class="font-medium text-gray-900">{{ currentTicket?.assignee.name || '未分配' }}</p>
                    <p class="text-sm text-gray-500">{{ currentTicket?.assignee.email || '-' }}</p>
                  </div>
                </div>
              </div>
            </div>

            <div class="mb-6">
              <h4 class="text-sm font-medium text-gray-500 mb-2">创建时间</h4>
              <p class="text-gray-900">{{ formatDate(currentTicket?.createdAt || new Date()) }}</p>
            </div>

            <div class="mb-6">
              <h4 class="text-sm font-medium text-gray-500 mb-2">附件</h4>
              <div v-if="currentTicket?.attachments && currentTicket.attachments.length > 0" class="space-y-2">
                <div v-for="attachment in currentTicket.attachments" :key="attachment.id" class="flex items-center justify-between bg-gray-50 p-3 rounded-lg">
                  <div class="flex items-center">
                    <i class="fas fa-paperclip text-gray-500 mr-2"></i>
                    <span class="text-sm text-gray-700">{{ attachment.name }}</span>
                    <span class="text-xs text-gray-500 ml-2">({{ formatFileSize(attachment.size) }})</span>
                  </div>
                  <button class="text-blue-600 hover:text-blue-800 text-sm">
                    <i class="fas fa-download mr-1"></i>下载
                  </button>
                </div>
              </div>
              <div v-else class="text-gray-500 text-sm">无附件</div>
            </div>

            <!-- 工单回复 -->
            <div class="mb-6">
              <h4 class="text-sm font-medium text-gray-500 mb-4">工单回复</h4>
              <div class="space-y-4">
                <div v-for="reply in currentTicket?.replies || []" :key="reply.id" class="border rounded-lg p-4">
                  <div class="flex justify-between items-start mb-2">
                    <div class="flex items-center">
                      <div class="w-8 h-8 rounded-full bg-blue-100 flex items-center justify-center mr-3">
                        <i class="fas fa-user text-blue-600"></i>
                      </div>
                      <div>
                        <p class="font-medium text-gray-900">{{ reply.author.name }}</p>
                        <p class="text-xs text-gray-500">{{ formatDate(reply.createdAt) }}</p>
                      </div>
                    </div>
                    <span class="px-2 py-1 text-xs rounded-full bg-gray-100 text-gray-800">{{ reply.type }}</span>
                  </div>
                  <div class="mt-2 p-3 bg-gray-50 rounded border-l-4 border-blue-500">
                    <p class="text-gray-800 whitespace-pre-line">{{ reply.content }}</p>
                  </div>
                </div>
              </div>

              <!-- 添加回复 -->
              <div v-if="canAddReply(currentTicket)" class="mt-6">
                <h5 class="text-sm font-medium text-gray-500 mb-2">添加回复</h5>
                <textarea 
                  v-model="newReplyContent"
                  rows="4"
                  class="w-full p-3 border border-gray-300 rounded-lg text-sm"
                  placeholder="请输入您的回复..."
                ></textarea>
                <div class="mt-3 flex justify-end space-x-3">
                  <button 
                    @click="addReply"
                    class="!rounded-button whitespace-nowrap px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700"
                    :disabled="!newReplyContent.trim()"
                  >
                    发送回复
                  </button>
                </div>
              </div>
            </div>

            <!-- 操作按钮 -->
            <div class="flex justify-end space-x-3 mt-6">
              <button 
                @click="closeTicketDetailModal"
                class="!rounded-button whitespace-nowrap px-4 py-2 border border-gray-300 text-gray-700 rounded-lg hover:bg-gray-50"
              >
                关闭
              </button>
              <button 
                v-if="canEditTicket(currentTicket)"
                @click="editTicket(currentTicket)"
                class="!rounded-button whitespace-nowrap px-4 py-2 bg-green-600 text-white rounded-lg hover:bg-green-700"
              >
                编辑工单
              </button>
              <button 
                v-if="canCloseTicket(currentTicket)"
                @click="closeTicket"
                class="!rounded-button whitespace-nowrap px-4 py-2 bg-red-600 text-white rounded-lg hover:bg-red-700"
              >
                关闭工单
              </button>
              <button 
                v-if="canStoreVector(currentTicket)"
                @click="storeVector"
                class="!rounded-button whitespace-nowrap px-4 py-2 bg-purple-600 text-white rounded-lg hover:bg-purple-700"
              >
                存储向量
              </button>
            </div>
          </div>
        </div>
      </div>

      <!-- 编辑工单模态框 -->
      <div v-if="showEditTicketModal" class="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50">
        <div class="bg-white rounded-lg shadow-xl w-1/2 max-w-2xl">
          <div class="p-6 border-b border-gray-200">
            <div class="flex justify-between items-center">
              <h3 class="text-lg font-semibold text-gray-800">编辑工单</h3>
              <button @click="closeEditTicketModal" class="text-gray-500 hover:text-gray-700">
                <i class="fas fa-times"></i>
              </button>
            </div>
          </div>
          <div class="p-6">
            <form @submit.prevent="saveEditedTicket">
              <div class="mb-4">
                <label class="block text-sm font-medium text-gray-700 mb-2">工单标题</label>
                <input 
                  v-model="editedTicket.title" 
                  type="text" 
                  class="w-full p-2 border border-gray-300 rounded-lg text-sm"
                  required
                />
              </div>
              
              <div class="mb-4">
                <label class="block text-sm font-medium text-gray-700 mb-2">工单描述</label>
                <textarea 
                  v-model="editedTicket.description"
                  class="w-full p-2 border border-gray-300 rounded-lg text-sm"
                  rows="4"
                  required
                ></textarea>
              </div>
              
              <div class="grid grid-cols-2 gap-4 mb-4">
                <div>
                  <label class="block text-sm font-medium text-gray-700 mb-2">工单类型</label>
                  <select 
                    v-model="editedTicket.type"
                    class="w-full p-2 border border-gray-300 rounded-lg text-sm"
                  >
                    <option value="INCIDENT">故障报告</option>
                    <option value="REQUEST">服务请求</option>
                    <option value="PROBLEM">问题分析</option>
                    <option value="CHANGE">变更请求</option>
                  </select>
                </div>
                <div>
                  <label class="block text-sm font-medium text-gray-700 mb-2">优先级</label>
                  <select 
                    v-model="editedTicket.priority"
                    class="w-full p-2 border border-gray-300 rounded-lg text-sm"
                  >
                    <option value="LOW">低</option>
                    <option value="MEDIUM">中</option>
                    <option value="HIGH">高</option>
                    <option value="CRITICAL">紧急</option>
                  </select>
                </div>
              </div>
              
              <div class="flex justify-end space-x-3 mt-6">
                <button 
                  type="button"
                  @click="closeEditTicketModal"
                  class="!rounded-button whitespace-nowrap px-4 py-2 border border-gray-300 text-gray-700 rounded-lg hover:bg-gray-50"
                >
                  取消
                </button>
                <button 
                  type="submit"
                  class="!rounded-button whitespace-nowrap px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700"
                >
                  保存更改
                </button>
              </div>
            </form>
          </div>
        </div>
      </div>

      <!-- 分配工单模态框 -->
      <div v-if="showAssignTicketModal" class="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50">
        <div class="bg-white rounded-lg shadow-xl w-1/3 max-w-md">
          <div class="p-6 border-b border-gray-200">
            <div class="flex justify-between items-center">
              <h3 class="text-lg font-semibold text-gray-800">分配工单 #{{ currentTicket?.id }}</h3>
              <button @click="closeAssignTicketModal" class="text-gray-500 hover:text-gray-700">
                <i class="fas fa-times"></i>
              </button>
            </div>
          </div>
          <div class="p-6">
            <form @submit.prevent="assignSelectedUser">
              <div class="mb-4">
                <label class="block text-sm font-medium text-gray-700 mb-2">选择负责人</label>
                <select 
                  v-model="selectedAssignee"
                  class="w-full p-2 border border-gray-300 rounded-lg text-sm"
                  required
                >
                  <option value="">请选择负责人</option>
                  <option v-for="user in users" :key="user.id" :value="user.id">
                    {{ user.name }}
                  </option>
                </select>
              </div>
              
              <div class="flex justify-end space-x-3 mt-6">
                <button 
                  type="button"
                  @click="closeAssignTicketModal"
                  class="!rounded-button whitespace-nowrap px-4 py-2 border border-gray-300 text-gray-700 rounded-lg hover:bg-gray-50"
                >
                  取消
                </button>
                <button 
                  type="submit"
                  class="!rounded-button whitespace-nowrap px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700"
                >
                  确认分配
                </button>
              </div>
            </form>
          </div>
        </div>
      </div>

      <!-- 状态更新模态框 -->
      <div v-if="showStatusUpdateModal" class="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50">
        <div class="bg-white rounded-lg shadow-xl w-1/3 max-w-md">
          <div class="p-6 border-b border-gray-200">
            <div class="flex justify-between items-center">
              <h3 class="text-lg font-semibold text-gray-800">更新工单状态 #{{ currentTicket?.id }}</h3>
              <button @click="closeStatusUpdateModal" class="text-gray-500 hover:text-gray-700">
                <i class="fas fa-times"></i>
              </button>
            </div>
          </div>
          <div class="p-6">
            <form @submit.prevent="updateSelectedStatus">
              <div class="mb-4">
                <label class="block text-sm font-medium text-gray-700 mb-2">选择新状态</label>
                <select 
                  v-model="selectedStatus"
                  class="w-full p-2 border border-gray-300 rounded-lg text-sm"
                  required
                >
                  <option value="">请选择状态</option>
                  <option value="ASSIGNED">已派单</option>
                  <option value="IN_PROGRESS">处理中</option>
                  <option value="COMPLETED">完成</option>
                  <option value="VECTOR_STORED">向量存储</option>
                </select>
              </div>
              
              <div class="mb-4">
                <label class="block text-sm font-medium text-gray-700 mb-2">备注</label>
                <textarea 
                  v-model="statusComment"
                  class="w-full p-2 border border-gray-300 rounded-lg text-sm"
                  rows="3"
                  placeholder="请输入状态更新的备注信息..."
                ></textarea>
              </div>
              
              <div class="flex justify-end space-x-3 mt-6">
                <button 
                  type="button"
                  @click="closeStatusUpdateModal"
                  class="!rounded-button whitespace-nowrap px-4 py-2 border border-gray-300 text-gray-700 rounded-lg hover:bg-gray-50"
                >
                  取消
                </button>
                <button 
                  type="submit"
                  class="!rounded-button whitespace-nowrap px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700"
                >
                  更新状态
                </button>
              </div>
            </form>
          </div>
        </div>
      </div>
    </div>
  </div>
</template>

<script lang="ts" setup>
import { ref, computed } from 'vue';

// 用户接口定义
interface User {
  id: string;
  name: string;
  email: string;
  role: 'admin' | 'staff' | 'manager';
}

// 工单接口定义
interface Ticket {
  id: string;
  title: string;
  description: string;
  type: 'INCIDENT' | 'REQUEST' | 'PROBLEM' | 'CHANGE';
  priority: 'LOW' | 'MEDIUM' | 'HIGH' | 'CRITICAL';
  status: 'ASSIGNED' | 'IN_PROGRESS' | 'COMPLETED' | 'VECTOR_STORED';
  submitter: User;
  assignee: User;
  createdAt: Date;
  updatedAt: Date;
  attachments: Attachment[];
  replies: Reply[];
}

interface Attachment {
  id: string;
  name: string;
  size: number;
  type: string;
  uploadedAt: Date;
}

interface Reply {
  id: string;
  author: User;
  content: string;
  type: 'COMMENT' | 'UPDATE' | 'RESOLUTION';
  createdAt: Date;
}

// 响应式数据
const showTicketDetailModal = ref(false);
const showEditTicketModal = ref(false);
const showAssignTicketModal = ref(false);
const showStatusUpdateModal = ref(false);
const currentTicket = ref<Ticket | null>(null);
const editedTicket = ref<Ticket | null>(null);
const newReplyContent = ref('');
const selectedAssignee = ref('');
const selectedStatus = ref('');
const statusComment = ref('');

// 筛选条件
const searchQuery = ref('');
const filterStatus = ref('');
const filterPriority = ref('');
const filterAssignee = ref('');
const filterType = ref('');

// 用户列表
const users: User[] = [
  { id: 'user-1', name: '张伟', email: 'zhangwei@example.com', role: 'staff' },
  { id: 'user-2', name: '李娜', email: 'lina@example.com', role: 'staff' },
  { id: 'user-3', name: '王强', email: 'wangqiang@example.com', role: 'staff' },
  { id: 'user-4', name: '陈丽', email: 'chenli@example.com', role: 'manager' },
  { id: 'user-5', name: '赵刚', email: 'zhaogang@example.com', role: 'admin' }
];

// 模拟工单数据
const tickets = ref<Ticket[]>([
  {
    id: 'TKT-001',
    title: 'API网关服务响应超时',
    description: '用户反馈API网关在高峰时段响应时间超过5秒，影响业务正常运行。初步判断可能是负载过高导致。',
    type: 'INCIDENT',
    priority: 'HIGH',
    status: 'IN_PROGRESS',
    submitter: { id: 'user-1', name: '张伟', email: 'zhangwei@example.com', role: 'staff' },
    assignee: { id: 'user-2', name: '李娜', email: 'lina@example.com', role: 'staff' },
    createdAt: new Date(Date.now() - 86400000 * 1),
    updatedAt: new Date(Date.now() - 3600000 * 2),
    attachments: [
      { id: 'att-1', name: 'error_logs.txt', size: 10240, type: 'text/plain', uploadedAt: new Date(Date.now() - 86400000 * 1) },
      { id: 'att-2', name: 'performance_report.pdf', size: 204800, type: 'application/pdf', uploadedAt: new Date(Date.now() - 86400000 * 1) }
    ],
    replies: [
      {
        id: 'rep-1',
        author: { id: 'user-2', name: '李娜', email: 'lina@example.com', role: 'staff' },
        content: '已收到工单，正在检查API网关的负载情况。初步发现是某个微服务响应过慢导致的连锁反应。',
        type: 'COMMENT',
        createdAt: new Date(Date.now() - 86400000 * 1 + 3600000 * 2)
      },
      {
        id: 'rep-2',
        author: { id: 'user-2', name: '李娜', email: 'lina@example.com', role: 'staff' },
        content: '已定位问题，是订单服务的数据库查询效率低下导致。已优化查询语句，系统响应恢复正常。',
        type: 'UPDATE',
        createdAt: new Date(Date.now() - 3600000 * 2)
      }
    ]
  },
  {
    id: 'TKT-002',
    title: '新增用户权限管理功能',
    description: '需要开发一套用户权限管理系统，支持角色分配、权限控制等功能。预计需要2周开发时间。',
    type: 'REQUEST',
    priority: 'MEDIUM',
    status: 'ASSIGNED',
    submitter: { id: 'user-3', name: '王强', email: 'wangqiang@example.com', role: 'staff' },
    assignee: { id: 'user-4', name: '陈丽', email: 'chenli@example.com', role: 'manager' },
    createdAt: new Date(Date.now() - 86400000 * 3),
    updatedAt: new Date(Date.now() - 86400000 * 1),
    attachments: [
      { id: 'att-3', name: 'requirement_doc.docx', size: 51200, type: 'application/vnd.openxmlformats-officedocument.wordprocessingml.document', uploadedAt: new Date(Date.now() - 86400000 * 3) }
    ],
    replies: [
      {
        id: 'rep-3',
        author: { id: 'user-4', name: '陈丽', email: 'chenli@example.com', role: 'manager' },
        content: '需求已确认，将安排开发团队进行评估和开发计划制定。',
        type: 'COMMENT',
        createdAt: new Date(Date.now() - 86400000 * 1)
      }
    ]
  },
  {
    id: 'TKT-003',
    title: '数据库连接池频繁耗尽',
    description: '生产环境数据库连接池经常达到上限，导致部分请求失败。需要进行容量规划和优化。',
    type: 'PROBLEM',
    priority: 'CRITICAL',
    status: 'COMPLETED',
    submitter: { id: 'user-5', name: '赵刚', email: 'zhaogang@example.com', role: 'admin' },
    assignee: { id: 'user-1', name: '张伟', email: 'zhangwei@example.com', role: 'staff' },
    createdAt: new Date(Date.now() - 86400000 * 7),
    updatedAt: new Date(Date.now() - 86400000 * 2),
    attachments: [
      { id: 'att-4', name: 'connection_pool_analysis.xlsx', size: 153600, type: 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet', uploadedAt: new Date(Date.now() - 86400000 * 7) }
    ],
    replies: [
      {
        id: 'rep-4',
        author: { id: 'user-1', name: '张伟', email: 'zhangwei@example.com', role: 'staff' },
        content: '已完成数据库连接池的分析，发现是某些长时间运行的查询占用了连接。已优化查询逻辑并增加了连接池大小。',
        type: 'RESOLUTION',
        createdAt: new Date(Date.now() - 86400000 * 2)
      }
    ]
  },
  {
    id: 'TKT-004',
    title: '部署新版本支付服务',
    description: '需要部署支付服务的新版本，包含多项安全修复和性能改进。计划在周末进行。',
    type: 'CHANGE',
    priority: 'HIGH',
    status: 'ASSIGNED',
    submitter: { id: 'user-2', name: '李娜', email: 'lina@example.com', role: 'staff' },
    assignee: { id: 'user-3', name: '王强', email: 'wangqiang@example.com', role: 'staff' },
    createdAt: new Date(Date.now() - 86400000 * 2),
    updatedAt: new Date(Date.now() - 86400000 * 1),
    attachments: [
      { id: 'att-5', name: 'deployment_plan.pdf', size: 81920, type: 'application/pdf', uploadedAt: new Date(Date.now() - 86400000 * 2) }
    ],
    replies: []
  },
  {
    id: 'TKT-005',
    title: '用户登录失败率异常升高',
    description: '近两天用户登录失败率显著上升，可能存在安全攻击或配置问题。需要紧急排查。',
    type: 'INCIDENT',
    priority: 'CRITICAL',
    status: 'IN_PROGRESS',
    submitter: { id: 'user-4', name: '陈丽', email: 'chenli@example.com', role: 'manager' },
    assignee: { id: 'user-1', name: '张伟', email: 'zhangwei@example.com', role: 'staff' },
    createdAt: new Date(Date.now() - 3600000 * 6),
    updatedAt: new Date(Date.now() - 3600000 * 1),
    attachments: [
      { id: 'att-6', name: 'login_attempts.csv', size: 256000, type: 'text/csv', uploadedAt: new Date(Date.now() - 3600000 * 6) }
    ],
    replies: [
      {
        id: 'rep-5',
        author: { id: 'user-1', name: '张伟', email: 'zhangwei@example.com', role: 'staff' },
        content: '初步分析发现大量来自特定IP段的登录尝试，疑似暴力破解攻击。已临时封禁相关IP并加强了登录验证。',
        type: 'UPDATE',
        createdAt: new Date(Date.now() - 3600000 * 1)
      }
    ]
  }
]);

// 计算属性：筛选后的工单
const filteredTickets = computed(() => {
  return tickets.value.filter(ticket => {
    const matchesSearch = !searchQuery.value || 
      ticket.title.toLowerCase().includes(searchQuery.value.toLowerCase()) ||
      ticket.description.toLowerCase().includes(searchQuery.value.toLowerCase()) ||
      ticket.id.toLowerCase().includes(searchQuery.value.toLowerCase());
    
    const matchesStatus = !filterStatus.value || ticket.status === filterStatus.value;
    const matchesPriority = !filterPriority.value || ticket.priority === filterPriority.value;
    const matchesAssignee = !filterAssignee.value || ticket.assignee.name === filterAssignee.value;
    const matchesType = !filterType.value || ticket.type === filterType.value;
    
    return matchesSearch && matchesStatus && matchesPriority && matchesAssignee && matchesType;
  });
});

// 获取状态文本
const getStatusText = (status: string) => {
  switch (status) {
    case 'ASSIGNED':
      return '已派单';
    case 'IN_PROGRESS':
      return '处理中';
    case 'COMPLETED':
      return '完成';
    case 'VECTOR_STORED':
      return '向量存储';
    default:
      return status;
  }
};

// 获取状态样式
const getStatusClass = (status: string) => {
  switch (status) {
    case 'ASSIGNED':
      return 'bg-blue-100 text-blue-800';
    case 'IN_PROGRESS':
      return 'bg-yellow-100 text-yellow-800';
    case 'COMPLETED':
      return 'bg-green-100 text-green-800';
    case 'VECTOR_STORED':
      return 'bg-purple-100 text-purple-800';
    default:
      return 'bg-gray-100 text-gray-800';
  }
};

// 获取优先级文本
const getPriorityText = (priority: string) => {
  switch (priority) {
    case 'LOW':
      return '低';
    case 'MEDIUM':
      return '中';
    case 'HIGH':
      return '高';
    case 'CRITICAL':
      return '紧急';
    default:
      return priority;
  }
};

// 获取优先级样式
const getPriorityClass = (priority: string) => {
  switch (priority) {
    case 'LOW':
      return 'bg-gray-100 text-gray-800';
    case 'MEDIUM':
      return 'bg-blue-100 text-blue-800';
    case 'HIGH':
      return 'bg-orange-100 text-orange-800';
    case 'CRITICAL':
      return 'bg-red-100 text-red-800';
    default:
      return 'bg-gray-100 text-gray-800';
  }
};

// 获取类型文本
const getTypeText = (type: string) => {
  switch (type) {
    case 'INCIDENT':
      return '故障报告';
    case 'REQUEST':
      return '服务请求';
    case 'PROBLEM':
      return '问题分析';
    case 'CHANGE':
      return '变更请求';
    default:
      return type;
  }
};

// 获取类型样式
const getTypeClass = (type: string) => {
  switch (type) {
    case 'INCIDENT':
      return 'bg-red-100 text-red-800';
    case 'REQUEST':
      return 'bg-green-100 text-green-800';
    case 'PROBLEM':
      return 'bg-yellow-100 text-yellow-800';
    case 'CHANGE':
      return 'bg-purple-100 text-purple-800';
    default:
      return 'bg-gray-100 text-gray-800';
  }
};

// 格式化日期
const formatDate = (date: Date) => {
  return new Date(date).toLocaleString('zh-CN');
};

// 格式化文件大小
const formatFileSize = (size: number) => {
  if (size < 1024) return size + ' B';
  if (size < 1048576) return (size / 1024).toFixed(1) + ' KB';
  return (size / 1048576).toFixed(1) + ' MB';
};

// 查看工单详情
const viewTicket = (ticket: Ticket) => {
  currentTicket.value = ticket;
  showTicketDetailModal.value = true;
  newReplyContent.value = '';
};

// 关闭工单详情模态框
const closeTicketDetailModal = () => {
  showTicketDetailModal.value = false;
  currentTicket.value = null;
  newReplyContent.value = '';
};

// 编辑工单
const editTicket = (ticket: Ticket) => {
  editedTicket.value = { ...ticket };
  showEditTicketModal.value = true;
};

// 保存编辑的工单
const saveEditedTicket = () => {
  if (!editedTicket.value) return;
  
  const index = tickets.value.findIndex(t => t.id === editedTicket.value?.id);
  if (index !== -1) {
    tickets.value[index] = { ...editedTicket.value, updatedAt: new Date() };
    closeEditTicketModal();
  }
};

// 关闭编辑工单模态框
const closeEditTicketModal = () => {
  showEditTicketModal.value = false;
  editedTicket.value = null;
};

// 分配工单
const assignTicket = (ticket: Ticket) => {
  currentTicket.value = ticket;
  selectedAssignee.value = ticket.assignee.id;
  showAssignTicketModal.value = true;
};

// 关闭分配工单模态框
const closeAssignTicketModal = () => {
  showAssignTicketModal.value = false;
  currentTicket.value = null;
  selectedAssignee.value = '';
};

// 确认分配用户
const assignSelectedUser = () => {
  if (!currentTicket.value || !selectedAssignee.value) return;
  
  const user = users.find(u => u.id === selectedAssignee.value);
  if (user) {
    const ticketIndex = tickets.value.findIndex(t => t.id === currentTicket.value?.id);
    if (ticketIndex !== -1) {
      tickets.value[ticketIndex].assignee = user;
      tickets.value[ticketIndex].status = 'ASSIGNED';
      tickets.value[ticketIndex].updatedAt = new Date();
      
      // 更新当前显示的工单
      if (currentTicket.value.id === tickets.value[ticketIndex].id) {
        currentTicket.value.assignee = user;
        currentTicket.value.status = 'ASSIGNED';
        currentTicket.value.updatedAt = new Date();
      }
    }
  }
  
  closeAssignTicketModal();
};

// 更新工单状态
const updateTicketStatus = (ticket: Ticket) => {
  currentTicket.value = ticket;
  selectedStatus.value = ticket.status;
  showStatusUpdateModal.value = true;
};

// 关闭状态更新模态框
const closeStatusUpdateModal = () => {
  showStatusUpdateModal.value = false;
  currentTicket.value = null;
  selectedStatus.value = '';
  statusComment.value = '';
};

// 更新选中的状态
const updateSelectedStatus = () => {
  if (!currentTicket.value || !selectedStatus.value) return;
  
  const ticketIndex = tickets.value.findIndex(t => t.id === currentTicket.value?.id);
  if (ticketIndex !== -1) {
    tickets.value[ticketIndex].status = selectedStatus.value as any;
    tickets.value[ticketIndex].updatedAt = new Date();
    
    // 添加状态更新回复
    if (statusComment.value.trim()) {
      const newReply: Reply = {
        id: `rep-${Date.now()}`,
        author: { id: 'user-current', name: '张伟', email: 'zhangwei@example.com', role: 'staff' },
        content: `状态更新: ${getStatusText(currentTicket.value.status)} -> ${getStatusText(selectedStatus.value as any)}\n备注: ${statusComment.value}`,
        type: 'UPDATE',
        createdAt: new Date()
      };
      tickets.value[ticketIndex].replies.push(newReply);
    }
    
    // 更新当前显示的工单
    if (currentTicket.value.id === tickets.value[ticketIndex].id) {
      currentTicket.value.status = selectedStatus.value as any;
      currentTicket.value.updatedAt = new Date();
    }
  }
  
  closeStatusUpdateModal();
};

// 添加回复
const addReply = () => {
  if (!currentTicket.value || !newReplyContent.value.trim()) return;
  
  const newReply: Reply = {
    id: `rep-${Date.now()}`,
    author: { id: 'user-current', name: '张伟', email: 'zhangwei@example.com', role: 'staff' },
    content: newReplyContent.value,
    type: 'COMMENT',
    createdAt: new Date()
  };
  
  const ticketIndex = tickets.value.findIndex(t => t.id === currentTicket.value?.id);
  if (ticketIndex !== -1) {
    tickets.value[ticketIndex].replies.push(newReply);
    tickets.value[ticketIndex].updatedAt = new Date();
    
    // 更新当前显示的工单
    if (currentTicket.value.id === tickets.value[ticketIndex].id) {
      currentTicket.value.replies.push(newReply);
      currentTicket.value.updatedAt = new Date();
    }
  }
  
  newReplyContent.value = '';
};

// 关闭工单
const closeTicket = () => {
  if (!currentTicket.value) return;
  
  const ticketIndex = tickets.value.findIndex(t => t.id === currentTicket.value?.id);
  if (ticketIndex !== -1) {
    tickets.value[ticketIndex].status = 'COMPLETED';
    tickets.value[ticketIndex].updatedAt = new Date();
    
    // 添加关闭回复
    const closeReply: Reply = {
      id: `rep-${Date.now()}`,
      author: { id: 'user-current', name: '张伟', email: 'zhangwei@example.com', role: 'staff' },
      content: '工单已关闭。问题已解决。',
      type: 'RESOLUTION',
      createdAt: new Date()
    };
    tickets.value[ticketIndex].replies.push(closeReply);
    
    // 更新当前显示的工单
    if (currentTicket.value.id === tickets.value[ticketIndex].id) {
      currentTicket.value.status = 'COMPLETED';
      currentTicket.value.updatedAt = new Date();
      currentTicket.value.replies.push(closeReply);
    }
  }
  
  alert('工单已关闭');
};

// 存储向量
const storeVector = () => {
  if (!currentTicket.value) return;
  
  const ticketIndex = tickets.value.findIndex(t => t.id === currentTicket.value?.id);
  if (ticketIndex !== -1) {
    tickets.value[ticketIndex].status = 'VECTOR_STORED';
    tickets.value[ticketIndex].updatedAt = new Date();
    
    // 添加存储回复
    const storeReply: Reply = {
      id: `rep-${Date.now()}`,
      author: { id: 'user-current', name: '张伟', email: 'zhangwei@example.com', role: 'staff' },
      content: '工单内容已存储至向量数据库，可用于后续智能问答。',
      type: 'UPDATE',
      createdAt: new Date()
    };
    tickets.value[ticketIndex].replies.push(storeReply);
    
    // 更新当前显示的工单
    if (currentTicket.value.id === tickets.value[ticketIndex].id) {
      currentTicket.value.status = 'VECTOR_STORED';
      currentTicket.value.updatedAt = new Date();
      currentTicket.value.replies.push(storeReply);
    }
  }
  
  alert('工单已存储至向量数据库');
};

// 创建新工单
const createNewTicket = () => {
  alert('新建工单功能将在后续版本中实现');
};

// 检查是否可以编辑工单
const canEditTicket = (ticket: Ticket | null) => {
  if (!ticket) return false;
  // 提交人可以编辑自己的工单
  return ticket.submitter.id === 'user-current' || ticket.submitter.name === '张伟';
};

// 检查是否可以分配工单
const canAssignTicket = (ticket: Ticket | null) => {
  if (!ticket) return false;
  // 管理员或当前负责人可以分配工单
  return ticket.submitter.role === 'admin' || ticket.assignee.role === 'admin' || ticket.submitter.role === 'manager';
};

// 检查是否可以更新状态
const canUpdateStatus = (ticket: Ticket | null) => {
  if (!ticket) return false;
  // 当前负责人或管理员可以更新状态
  return ticket.assignee.id === 'user-current' || ticket.assignee.name === '张伟' || ticket.submitter.role === 'admin';
};

// 检查是否可以添加回复
const canAddReply = (ticket: Ticket | null) => {
  if (!ticket) return false;
  // 当前负责人、提交人或管理员可以添加回复
  return (
    ticket.assignee.id === 'user-current' || 
    ticket.assignee.name === '张伟' || 
    ticket.submitter.id === 'user-current' || 
    ticket.submitter.name === '张伟' || 
    ticket.submitter.role === 'admin'
  );
};

// 检查是否可以关闭工单
const canCloseTicket = (ticket: Ticket | null) => {
  if (!ticket) return false;
  // 当前负责人或管理员可以关闭工单
  return ticket.assignee.id === 'user-current' || ticket.assignee.name === '张伟' || ticket.submitter.role === 'admin';
};

// 检查是否可以存储向量
const canStoreVector = (ticket: Ticket | null) => {
  if (!ticket) return false;
  // 只有完成状态的工单才能存储向量，且只有管理员可以操作
  return ticket.status === 'COMPLETED' && ticket.submitter.role === 'admin';
};
</script>

<style scoped>
/* 自定义按钮圆角样式 */
.rounded-button {
  border-radius: 8px;
}

.line-clamp-1 {
  display: -webkit-box;
  -webkit-line-clamp: 1;
  -webkit-box-orient: vertical;
  overflow: hidden;
}
</style>

