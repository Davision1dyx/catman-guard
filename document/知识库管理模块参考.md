<!-- 代码已包含 CSS：使用 TailwindCSS , 安装 TailwindCSS 后方可看到布局样式效果 -->

<template>
  <div class="min-h-screen bg-gray-50 flex">
    <!-- 侧边导航栏 -->
    <div class="w-64 bg-white shadow-lg h-screen fixed left-0 top-0 overflow-y-auto">
      <!-- Logo 区 -->
      <div class="p-6 border-b border-gray-200">
        <div class="flex items-center space-x-3">
          <div class="bg-blue-600 p-2 rounded-lg">
            <i class="fas fa-brain text-white text-xl"></i>
          </div>
          <h1 class="text-xl font-bold text-gray-800">智维 Agent</h1>
        </div>
      </div>

      <!-- 核心菜单 -->
      <nav class="p-4">
        <ul class="space-y-2">
          <li>
            <a href="#" class="flex items-center p-3 rounded-lg hover:bg-gray-100 text-gray-700">
              <i class="fas fa-comments mr-3"></i>
              <span>智能问答</span>
            </a>
          </li>
          <li>
            <a href="#" class="flex items-center p-3 rounded-lg bg-blue-50 text-blue-600">
              <i class="fas fa-book mr-3"></i>
              <span>知识库管理</span>
            </a>
          </li>
          <li>
            <a href="#" class="flex items-center p-3 rounded-lg hover:bg-gray-100 text-gray-700">
              <i class="fas fa-tasks mr-3"></i>
              <span>工单中心</span>
            </a>
          </li>
          <li>
            <a href="#" class="flex items-center p-3 rounded-lg hover:bg-gray-100 text-gray-700">
              <i class="fas fa-chart-bar mr-3"></i>
              <span>数据看板</span>
            </a>
          </li>
          <li>
            <a href="#" class="flex items-center p-3 rounded-lg hover:bg-gray-100 text-gray-700">
              <i class="fas fa-calendar-alt mr-3"></i>
              <span>值班排班</span>
            </a>
          </li>
          <li>
            <a href="#" class="flex items-center p-3 rounded-lg hover:bg-gray-100 text-gray-700">
              <i class="fas fa-cog mr-3"></i>
              <span>系统设置</span>
            </a>
          </li>
        </ul>
      </nav>

      <!-- 用户信息区 -->
      <div class="absolute bottom-0 w-full p-4 border-t border-gray-200">
        <div class="flex items-center space-x-3">
          <div class="w-10 h-10 rounded-full bg-blue-100 flex items-center justify-center">
            <i class="fas fa-user text-blue-600"></i>
          </div>
          <div>
            <p class="font-medium text-gray-800">张伟</p>
            <p class="text-xs text-gray-500">值班人员</p>
          </div>
          <div class="ml-auto">
            <div class="w-3 h-3 rounded-full bg-green-500"></div>
          </div>
        </div>
      </div>
    </div>

    <!-- 主内容区 -->
    <div class="ml-64 flex-1 min-h-screen">
      <!-- 顶部状态栏 -->
      <header class="bg-white shadow-sm p-4">
        <div class="flex items-center justify-between">
          <div class="flex items-center space-x-4">
            <nav class="flex" aria-label="Breadcrumb">
              <ol class="inline-flex items-center space-x-1 md:space-x-3">
                <li class="inline-flex items-center">
                  <i class="fas fa-home text-gray-400 mr-1"></i>
                  <span class="text-gray-700">首页</span>
                </li>
                <li>
                  <div class="flex items-center">
                    <i class="fas fa-chevron-right text-gray-400 mx-2 text-xs"></i>
                    <span class="text-gray-700">知识库管理</span>
                  </div>
                </li>
              </ol>
            </nav>
          </div>
          <div class="flex items-center space-x-4">
            <!-- 全局搜索 -->
            <div class="relative">
              <input
                type="text"
                placeholder="搜索工单、文档或对话..."
                class="pl-10 pr-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent text-sm"
              />
              <i class="fas fa-search absolute left-3 top-3 text-gray-400"></i>
            </div>
            <!-- 通知中心 -->
            <button class="relative p-2 text-gray-500 hover:text-gray-700">
              <i class="fas fa-bell"></i>
              <span class="absolute top-0 right-0 w-4 h-4 bg-red-500 text-white text-xs rounded-full flex items-center justify-center">3</span>
            </button>
          </div>
        </div>
      </header>

      <!-- 主工作区 -->
      <main class="p-6">
        <!-- 页面头部 -->
        <div class="mb-8">
          <div class="flex justify-between items-center">
            <h1 class="text-2xl font-bold text-gray-800">知识库管理</h1>
            <div class="flex space-x-3">
              <button 
                @click="showCreateKnowledgeBaseModal = true"
                class="!rounded-button whitespace-nowrap px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 flex items-center"
              >
                <i class="fas fa-plus mr-2"></i>
                创建知识库
              </button>
            </div>
          </div>
          <p class="text-gray-600 mt-2">管理和维护多个知识库，每个知识库包含相关文档集合</p>
        </div>

        <!-- 知识库列表 -->
        <div class="bg-white rounded-lg shadow-sm border border-gray-200 overflow-hidden mb-8">
          <div class="p-6 border-b border-gray-200">
            <h2 class="text-lg font-semibold text-gray-800">知识库列表</h2>
          </div>
          
          <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 p-6">
            <div 
              v-for="kb in knowledgeBases" 
              :key="kb.id" 
              class="border rounded-lg p-5 hover:shadow-md transition-shadow cursor-pointer"
              :class="{ 'ring-2 ring-blue-500': kb.id === currentKnowledgeBaseId }"
              @click="switchKnowledgeBase(kb.id)"
            >
              <div class="flex justify-between items-start">
                <div>
                  <h3 class="font-semibold text-lg text-gray-800">{{ kb.name }}</h3>
                  <p class="text-sm text-gray-500 mt-1">{{ kb.description }}</p>
                </div>
                <div class="flex space-x-2">
                  <button 
                    @click.stop="editKnowledgeBase(kb)"
                    class="text-gray-500 hover:text-blue-600"
                    title="编辑知识库"
                  >
                    <i class="fas fa-edit"></i>
                  </button>
                  <button 
                    @click.stop="deleteKnowledgeBase(kb)"
                    class="text-gray-500 hover:text-red-600"
                    title="删除知识库"
                  >
                    <i class="fas fa-trash"></i>
                  </button>
                </div>
              </div>
              <div class="mt-4 flex justify-between text-sm">
                <div class="flex items-center">
                  <i class="fas fa-file text-blue-500 mr-1"></i>
                  <span>{{ kb.fileCount }} 个文件</span>
                </div>
                <div class="flex items-center">
                  <i class="fas fa-database text-green-500 mr-1"></i>
                  <span>{{ kb.chunkCount }} 个分片</span>
                </div>
              </div>
              <div class="mt-3">
                <span class="inline-block px-2 py-1 text-xs rounded-full bg-blue-100 text-blue-800">
                  {{ getStatusText(kb.status) }}
                </span>
              </div>
            </div>
            
            <div 
              class="border-2 border-dashed border-gray-300 rounded-lg p-8 flex flex-col items-center justify-center hover:border-blue-400 cursor-pointer"
              @click="showCreateKnowledgeBaseModal = true"
            >
              <i class="fas fa-plus text-gray-400 text-2xl mb-2"></i>
              <p class="text-gray-500 text-center">创建新的知识库</p>
            </div>
          </div>
        </div>

        <!-- 当前知识库操作区 -->
        <div v-if="currentKnowledgeBaseId" class="bg-white rounded-lg shadow-sm border border-gray-200 overflow-hidden mb-8">
          <div class="p-6 border-b border-gray-200 flex justify-between items-center">
            <div>
              <h2 class="text-lg font-semibold text-gray-800">{{ currentKnowledgeBase?.name }}</h2>
              <p class="text-sm text-gray-500">{{ currentKnowledgeBase?.description }}</p>
            </div>
            <div class="flex space-x-3">
              <button 
                @click="showUploadModal = true"
                class="!rounded-button whitespace-nowrap px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 flex items-center"
              >
                <i class="fas fa-file-upload mr-2"></i>
                上传文件
              </button>
              <button 
                @click="syncKnowledgeBase"
                class="!rounded-button whitespace-nowrap px-4 py-2 bg-green-600 text-white rounded-lg hover:bg-green-700 flex items-center"
              >
                <i class="fas fa-sync mr-2"></i>
                同步向量库
              </button>
            </div>
          </div>
          
          <!-- 文件列表 -->
          <div class="overflow-x-auto">
            <table class="min-w-full divide-y divide-gray-200">
              <thead class="bg-gray-50">
                <tr>
                  <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">文件标题</th>
                  <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">软件版本</th>
                  <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">特性</th>
                  <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">微服务</th>
                  <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">状态</th>
                  <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">大小</th>
                  <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">上传时间</th>
                  <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">操作</th>
                </tr>
              </thead>
              <tbody class="bg-white divide-y divide-gray-200">
                <tr v-for="file in filteredFiles" :key="file.id" class="hover:bg-gray-50">
                  <td class="px-6 py-4 whitespace-nowrap">
                    <div class="flex items-center">
                      <i class="fas fa-file text-blue-500 mr-3"></i>
                      <div>
                        <div class="text-sm font-medium text-gray-900">{{ file.title }}</div>
                        <div class="text-sm text-gray-500">{{ file.originalName }}</div>
                      </div>
                    </div>
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">{{ file.softwareVersion || '-' }}</td>
                  <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">{{ file.feature || '-' }}</td>
                  <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">{{ file.microservice || '-' }}</td>
                  <td class="px-6 py-4 whitespace-nowrap">
                    <span class="px-2 py-1 text-xs rounded-full" :class="getStatusClass(file.status)">
                      {{ getStatusText(file.status) }}
                    </span>
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">{{ formatFileSize(file.size) }}</td>
                  <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">{{ formatDate(file.uploadTime) }}</td>
                  <td class="px-6 py-4 whitespace-nowrap text-sm">
                    <div class="flex space-x-2">
                      <button 
                        @click="downloadFile(file)"
                        class="text-blue-600 hover:text-blue-900"
                        :title="'下载 ' + file.title"
                      >
                        <i class="fas fa-download"></i>
                      </button>
                      <button 
                        @click="viewChunks(file)"
                        class="text-green-600 hover:text-green-900"
                        :title="'查看分片'"
                        :disabled="!canViewChunks(file)"
                      >
                        <i class="fas fa-list"></i>
                      </button>
                      <button 
                        @click="openChunkVisualization(file)"
                        class="text-purple-600 hover:text-purple-900"
                        :title="'可视化预览'"
                        :disabled="!canViewChunks(file)"
                      >
                        <i class="fas fa-eye"></i>
                      </button>
                      <button 
                        @click="openChunkModal(file)"
                        class="text-purple-600 hover:text-purple-900"
                        :title="'分片处理'"
                        :disabled="!canChunk(file)"
                      >
                        <i class="fas fa-cut"></i>
                      </button>
                      <button 
                        @click="deleteFile(file)"
                        class="text-red-600 hover:text-red-900"
                        :title="'删除文件'"
                      >
                        <i class="fas fa-trash"></i>
                      </button>
                    </div>
                  </td>
                </tr>
              </tbody>
            </table>
          </div>
          
          <div v-if="filteredFiles.length === 0" class="p-12 text-center">
            <i class="fas fa-folder-open text-gray-300 text-4xl mb-4"></i>
            <p class="text-gray-500">该知识库中暂无文件，请上传第一个知识库文档</p>
          </div>
        </div>

        <!-- 创建/编辑知识库模态框 -->
        <div v-if="showCreateKnowledgeBaseModal" class="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50">
          <div class="bg-white rounded-lg shadow-xl w-1/2 max-w-2xl">
            <div class="p-6 border-b border-gray-200">
              <div class="flex justify-between items-center">
                <h3 class="text-lg font-semibold text-gray-800">{{ editingKnowledgeBase ? '编辑知识库' : '创建新知识库' }}</h3>
                <button @click="closeKnowledgeBaseModal" class="text-gray-500 hover:text-gray-700">
                  <i class="fas fa-times"></i>
                </button>
              </div>
            </div>
            <div class="p-6">
              <form @submit.prevent="saveKnowledgeBase">
                <div class="mb-4">
                  <label class="block text-sm font-medium text-gray-700 mb-2">知识库名称</label>
                  <input 
                    v-model="newKnowledgeBase.name" 
                    type="text" 
                    class="w-full p-2 border border-gray-300 rounded-lg text-sm"
                    required
                    placeholder="请输入知识库名称"
                  />
                </div>
                
                <div class="mb-4">
                  <label class="block text-sm font-medium text-gray-700 mb-2">知识库描述</label>
                  <textarea 
                    v-model="newKnowledgeBase.description"
                    class="w-full p-2 border border-gray-300 rounded-lg text-sm"
                    rows="3"
                    placeholder="请输入知识库描述"
                  ></textarea>
                </div>
                
                <div class="mb-4">
                  <label class="block text-sm font-medium text-gray-700 mb-2">知识库类型</label>
                  <select 
                    v-model="newKnowledgeBase.type"
                    class="w-full p-2 border border-gray-300 rounded-lg text-sm"
                  >
                    <option value="technical">技术文档</option>
                    <option value="product">产品文档</option>
                    <option value="operation">运维文档</option>
                    <option value="training">培训资料</option>
                  </select>
                </div>
                
                <div class="flex justify-end space-x-3 mt-6">
                  <button 
                    type="button"
                    @click="closeKnowledgeBaseModal"
                    class="!rounded-button whitespace-nowrap px-4 py-2 border border-gray-300 text-gray-700 rounded-lg hover:bg-gray-50"
                  >
                    取消
                  </button>
                  <button 
                    type="submit"
                    class="!rounded-button whitespace-nowrap px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700"
                  >
                    {{ editingKnowledgeBase ? '保存更改' : '创建知识库' }}
                  </button>
                </div>
              </form>
            </div>
          </div>
        </div>

        <!-- 文件上传模态框 -->
        <div v-if="showUploadModal" class="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50">
          <div class="bg-white rounded-lg shadow-xl w-1/2 max-w-2xl">
            <div class="p-6 border-b border-gray-200">
              <div class="flex justify-between items-center">
                <h3 class="text-lg font-semibold text-gray-800">上传新文件到 "{{ currentKnowledgeBase?.name }}"</h3>
                <button @click="closeUploadModal" class="text-gray-500 hover:text-gray-700">
                  <i class="fas fa-times"></i>
                </button>
              </div>
            </div>
            <div class="p-6">
              <form @submit.prevent="uploadFile">
                <div class="mb-4">
                  <label class="block text-sm font-medium text-gray-700 mb-2">文件标题</label>
                  <input 
                    v-model="newFile.title" 
                    type="text" 
                    class="w-full p-2 border border-gray-300 rounded-lg text-sm"
                    required
                  />
                </div>
                
                <div class="grid grid-cols-2 gap-4 mb-4">
                  <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">软件版本</label>
                    <input 
                      v-model="newFile.softwareVersion" 
                      type="text" 
                      class="w-full p-2 border border-gray-300 rounded-lg text-sm"
                    />
                  </div>
                  <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">所属特性</label>
                    <input 
                      v-model="newFile.feature" 
                      type="text" 
                      class="w-full p-2 border border-gray-300 rounded-lg text-sm"
                    />
                  </div>
                </div>
                
                <div class="mb-4">
                  <label class="block text-sm font-medium text-gray-700 mb-2">微服务</label>
                  <input 
                    v-model="newFile.microservice" 
                    type="text" 
                    class="w-full p-2 border border-gray-300 rounded-lg text-sm"
                  />
                </div>
                
                <div class="mb-4">
                  <label class="block text-sm font-medium text-gray-700 mb-2">选择文件</label>
                  <div 
                    @dragover.prevent="handleDragOver"
                    @drop.prevent="handleDrop"
                    @click="fileInputRef?.click()"
                    class="border-2 border-dashed border-gray-300 rounded-lg p-8 text-center cursor-pointer hover:border-blue-400 transition-colors"
                  >
                    <i class="fas fa-cloud-upload-alt text-gray-400 text-3xl mb-2"></i>
                    <p class="text-gray-600">点击或拖拽文件到此处上传</p>
                    <p class="text-sm text-gray-500 mt-1">支持 PDF, DOC, DOCX, TXT, MD 等格式</p>
                    <input 
                      ref="fileInputRef"
                      type="file" 
                      @change="handleFileSelect"
                      class="hidden"
                      accept=".pdf,.doc,.docx,.txt,.md"
                    />
                  </div>
                </div>
                
                <div class="flex justify-end space-x-3 mt-6">
                  <button 
                    type="button"
                    @click="closeUploadModal"
                    class="!rounded-button whitespace-nowrap px-4 py-2 border border-gray-300 text-gray-700 rounded-lg hover:bg-gray-50"
                  >
                    取消
                  </button>
                  <button 
                    type="submit"
                    class="!rounded-button whitespace-nowrap px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700"
                    :disabled="!selectedFile"
                  >
                    上传文件
                  </button>
                </div>
              </form>
            </div>
          </div>
        </div>

        <!-- 分片操作模态框 -->
        <div v-if="showChunkModal" class="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50">
          <div class="bg-white rounded-lg shadow-xl w-1/2 max-w-2xl">
            <div class="p-6 border-b border-gray-200">
              <div class="flex justify-between items-center">
                <h3 class="text-lg font-semibold text-gray-800">分片操作 - {{ selectedFileForChunk?.title }}</h3>
                <button @click="closeChunkModal" class="text-gray-500 hover:text-gray-700">
                  <i class="fas fa-times"></i>
                </button>
              </div>
            </div>
            <div class="p-6">
              <div class="mb-6">
                <div class="flex items-center space-x-4 mb-4">
                  <div class="flex-1">
                    <label class="block text-sm font-medium text-gray-700 mb-2">分片模式</label>
                    <select 
                      v-model="chunkMode"
                      class="w-full p-2 border border-gray-300 rounded-lg text-sm"
                    >
                      <option value="fixed_length">固定长度分片</option>
                      <option value="parent_child">父子分片</option>
                      <option value="semantic">语义分片</option>
                    </select>
                  </div>
                  <div v-if="chunkMode === 'fixed_length'" class="flex-1">
                    <label class="block text-sm font-medium text-gray-700 mb-2">分片长度</label>
                    <input 
                      v-model.number="chunkLength"
                      type="number" 
                      class="w-full p-2 border border-gray-300 rounded-lg text-sm"
                      min="100"
                      max="5000"
                    />
                  </div>
                </div>
                
                <div v-if="chunkMode === 'parent_child'" class="mb-4">
                  <label class="block text-sm font-medium text-gray-700 mb-2">子块长度</label>
                  <input 
                    v-model.number="childChunkLength"
                    type="number" 
                    class="w-full p-2 border border-gray-300 rounded-lg text-sm"
                    min="50"
                    max="2000"
                  />
                </div>
              </div>
              
              <div class="flex justify-end space-x-3">
                <button 
                  @click="closeChunkModal"
                  class="!rounded-button whitespace-nowrap px-4 py-2 border border-gray-300 text-gray-700 rounded-lg hover:bg-gray-50"
                >
                  取消
                </button>
                <button 
                  @click="performChunking"
                  class="!rounded-button whitespace-nowrap px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700"
                >
                  开始分片
                </button>
              </div>
            </div>
          </div>
        </div>

        <!-- 分片结果详情模态框 -->
        <div v-if="showChunkDetailModal" class="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50">
          <div class="bg-white rounded-lg shadow-xl w-2/3 max-w-4xl max-h-[80vh] overflow-y-auto">
            <div class="p-6 border-b border-gray-200">
              <div class="flex justify-between items-center">
                <h3 class="text-lg font-semibold text-gray-800">分片结果 - {{ selectedFileForChunk?.title }}</h3>
                <button @click="closeChunkDetailModal" class="text-gray-500 hover:text-gray-700">
                  <i class="fas fa-times"></i>
                </button>
              </div>
            </div>
            <div class="p-6">
              <div class="mb-4">
                <div class="grid grid-cols-4 gap-4 mb-4">
                  <div class="bg-blue-50 p-4 rounded-lg">
                    <p class="text-sm text-blue-600">总分片数</p>
                    <p class="text-2xl font-bold text-blue-800">{{ chunkDetails.chunks.length }}</p>
                  </div>
                  <div class="bg-green-50 p-4 rounded-lg">
                    <p class="text-sm text-green-600">最大分片</p>
                    <p class="text-2xl font-bold text-green-800">{{ chunkDetails.maxChunkSize }} 字符</p>
                  </div>
                  <div class="bg-purple-50 p-4 rounded-lg">
                    <p class="text-sm text-purple-600">最小分片</p>
                    <p class="text-2xl font-bold text-purple-800">{{ chunkDetails.minChunkSize }} 字符</p>
                  </div>
                  <div class="bg-yellow-50 p-4 rounded-lg">
                    <p class="text-sm text-yellow-600">平均分片</p>
                    <p class="text-2xl font-bold text-yellow-800">{{ chunkDetails.avgChunkSize }} 字符</p>
                  </div>
                </div>
                
                <div class="overflow-x-auto">
                  <table class="min-w-full divide-y divide-gray-200">
                    <thead class="bg-gray-50">
                      <tr>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">序号</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">ID</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">长度</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">内容预览</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">操作</th>
                      </tr>
                    </thead>
                    <tbody class="bg-white divide-y divide-gray-200">
                      <tr v-for="(chunk, index) in chunkDetails.chunks" :key="index">
                        <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">{{ index + 1 }}</td>
                        <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">{{ chunk.id }}</td>
                        <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">{{ chunk.size }}</td>
                        <td class="px-6 py-4 text-sm text-gray-900 max-w-xs truncate">{{ chunk.content.substring(0, 100) }}{{ chunk.content.length > 100 ? '...' : '' }}</td>
                        <td class="px-6 py-4 whitespace-nowrap text-sm">
                          <button 
                            @click="viewChunkDetail(chunk)"
                            class="text-blue-600 hover:text-blue-900 mr-3"
                          >
                            <i class="fas fa-eye"></i>
                          </button>
                          <button 
                            @click="copyToClipboard(chunk.content)"
                            class="text-green-600 hover:text-green-900"
                          >
                            <i class="fas fa-copy"></i>
                          </button>
                        </td>
                      </tr>
                    </tbody>
                  </table>
                </div>
              </div>
            </div>
          </div>
        </div>

        <!-- 分片可视化模态框 -->
        <div v-if="showChunkVisualizationModal" class="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50">
          <div class="bg-white rounded-lg shadow-xl w-4/5 max-w-6xl max-h-[90vh] flex flex-col">
            <div class="p-6 border-b border-gray-200 flex justify-between items-center">
              <h3 class="text-lg font-semibold text-gray-800">分片可视化 - {{ selectedFileForChunk?.title }}</h3>
              <button @click="closeChunkVisualizationModal" class="text-gray-500 hover:text-gray-700">
                <i class="fas fa-times"></i>
              </button>
            </div>
            <div class="p-6 flex-1 overflow-y-auto">
              <div class="mb-4">
                <h4 class="text-md font-medium text-gray-700 mb-2">原文档内容</h4>
                <div class="bg-gray-100 p-4 rounded-lg border">
                  <div class="whitespace-pre-wrap leading-relaxed" v-html="highlightedContent"></div>
                </div>
              </div>
              <div class="mt-6">
                <h4 class="text-md font-medium text-gray-700 mb-2">分片详情</h4>
                <div class="space-y-4">
                  <div v-for="(chunk, index) in chunkDetails.chunks" :key="index" class="border rounded-lg p-4">
                    <div class="flex justify-between items-start">
                      <div>
                        <span class="font-medium text-gray-700">分片 {{ index + 1 }}:</span>
                        <span class="text-sm text-gray-500 ml-2">(长度: {{ chunk.size }})</span>
                      </div>
                      <button 
                        @click="copyToClipboard(chunk.content)"
                        class="text-green-600 hover:text-green-900 text-sm"
                      >
                        <i class="fas fa-copy mr-1"></i>复制
                      </button>
                    </div>
                    <div class="mt-2 p-3 bg-gray-50 rounded border-l-4 border-blue-500">
                      <div class="whitespace-pre-line text-gray-800">{{ chunk.content }}</div>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </main>
    </div>
  </div>
</template>

<script lang="ts" setup>
import { ref, computed } from 'vue';

// 知识库接口定义
interface KnowledgeBase {
  id: string;
  name: string;
  description: string;
  type: 'technical' | 'product' | 'operation' | 'training';
  status: 'ACTIVE' | 'INACTIVE' | 'MAINTENANCE';
  fileCount: number;
  chunkCount: number;
  createdAt: Date;
  updatedAt: Date;
}

// 文件接口定义
interface FileItem {
  id: string;
  title: string;
  originalName: string;
  softwareVersion?: string;
  feature?: string;
  microservice?: string;
  status: 'INIT' | 'UPLOADED' | 'CONVERTING' | 'CONVERTED' | 'CHUNKED' | 'VECTOR_STORED';
  size: number;
  uploadTime: Date;
  knowledgeBaseId: string;
  chunks?: Chunk[];
}

interface Chunk {
  id: string;
  content: string;
  size: number;
  metadata?: any;
}

// 响应式数据
const showCreateKnowledgeBaseModal = ref(false);
const showUploadModal = ref(false);
const showChunkModal = ref(false);
const showChunkDetailModal = ref(false);
const showChunkVisualizationModal = ref(false);
const fileInputRef = ref<HTMLInputElement | null>(null);

const newKnowledgeBase = ref({
  name: '',
  description: '',
  type: 'technical' as 'technical' | 'product' | 'operation' | 'training'
});

const newFile = ref({
  title: '',
  softwareVersion: '',
  feature: '',
  microservice: ''
});

const editingKnowledgeBase = ref<KnowledgeBase | null>(null);
const selectedFile = ref<File | null>(null);
const selectedFileForChunk = ref<FileItem | null>(null);
const currentKnowledgeBaseId = ref<string | null>(null);
const chunkMode = ref<'fixed_length' | 'parent_child' | 'semantic'>('fixed_length');
const chunkLength = ref(1000);
const childChunkLength = ref(200);

const chunkDetails = ref({
  chunks: [] as Chunk[],
  maxChunkSize: 0,
  minChunkSize: 0,
  avgChunkSize: 0
});

// 模拟知识库数据
const knowledgeBases = ref<KnowledgeBase[]>([
  {
    id: 'kb-1',
    name: 'API 网关文档',
    description: '包含API网关的配置指南、使用说明和最佳实践',
    type: 'technical',
    status: 'ACTIVE',
    fileCount: 12,
    chunkCount: 145,
    createdAt: new Date(Date.now() - 86400000 * 30),
    updatedAt: new Date()
  },
  {
    id: 'kb-2',
    name: '数据库运维手册',
    description: '数据库性能优化、备份恢复、故障排查等相关文档',
    type: 'operation',
    status: 'ACTIVE',
    fileCount: 8,
    chunkCount: 98,
    createdAt: new Date(Date.now() - 86400000 * 45),
    updatedAt: new Date(Date.now() - 86400000 * 5)
  },
  {
    id: 'kb-3',
    name: '用户服务规范',
    description: '用户管理服务的开发规范和接口文档',
    type: 'technical',
    status: 'MAINTENANCE',
    fileCount: 5,
    chunkCount: 67,
    createdAt: new Date(Date.now() - 86400000 * 20),
    updatedAt: new Date(Date.now() - 86400000 * 10)
  },
  {
    id: 'kb-4',
    name: '产品功能说明',
    description: '产品各功能模块的详细说明和用户手册',
    type: 'product',
    status: 'ACTIVE',
    fileCount: 15,
    chunkCount: 210,
    createdAt: new Date(Date.now() - 86400000 * 60),
    updatedAt: new Date()
  }
]);

// 模拟文件数据
const files = ref<FileItem[]>([
  {
    id: '1',
    title: 'API Gateway 配置指南',
    originalName: 'api_gateway_config_guide.pdf',
    softwareVersion: 'v2.3.1',
    feature: '认证授权',
    microservice: 'auth-service',
    status: 'CHUNKED',
    size: 2048576,
    uploadTime: new Date(Date.now() - 86400000),
    knowledgeBaseId: 'kb-1',
    chunks: [
      { id: 'chunk-1-1', content: 'API Gateway 是微服务架构中的关键组件，负责请求路由、认证授权、限流等功能。它作为系统的入口点，对外提供统一的API接口，内部则根据路由规则将请求转发到相应的微服务。', size: 1024 },
      { id: 'chunk-1-2', content: '配置 API Gateway 的第一步是设置路由规则，指定目标服务的地址和路径。路由规则通常包括路径匹配、方法限制、参数校验等配置项。此外，还需要配置负载均衡策略，以便在多个实例间分配请求流量。', size: 950 },
      { id: 'chunk-1-3', content: '认证授权功能通过 JWT Token 实现，客户端需要在请求头中携带有效的 token。API Gateway 会验证 token 的有效性，并解析其中的用户身份信息。根据用户的权限级别，决定是否允许访问特定的资源或执行特定的操作。', size: 1100 }
    ]
  },
  {
    id: '2',
    title: '数据库性能优化手册',
    originalName: 'db_performance_optimization.docx',
    softwareVersion: 'v1.8.5',
    feature: '性能调优',
    microservice: 'data-service',
    status: 'CONVERTED',
    size: 3145728,
    uploadTime: new Date(Date.now() - 172800000),
    knowledgeBaseId: 'kb-2',
    chunks: []
  },
  {
    id: '3',
    title: '用户服务开发规范',
    originalName: 'user_service_dev_guidelines.md',
    softwareVersion: 'v3.1.2',
    feature: '用户管理',
    microservice: 'user-service',
    status: 'UPLOADED',
    size: 1048576,
    uploadTime: new Date(Date.now() - 259200000),
    knowledgeBaseId: 'kb-3',
    chunks: []
  },
  {
    id: '4',
    title: '支付模块安全说明',
    originalName: 'payment_module_security.pdf',
    softwareVersion: 'v4.0.0',
    feature: '支付功能',
    microservice: 'payment-service',
    status: 'VECTOR_STORED',
    size: 1572864,
    uploadTime: new Date(Date.now() - 345600000),
    knowledgeBaseId: 'kb-1',
    chunks: [
      { id: 'chunk-4-1', content: '支付模块的安全性至关重要，需要实施多层次的安全防护措施。这包括数据传输加密、敏感信息脱敏、交易风险监控等多个方面。', size: 1200 },
      { id: 'chunk-4-2', content: '敏感信息加密是支付模块的基础安全要求，所有交易数据必须经过加密处理。采用行业标准的加密算法，如 AES-256，确保即使数据被截获也无法轻易解密。', size: 1150 }
    ]
  },
  {
    id: '5',
    title: '产品功能介绍',
    originalName: 'product_features_overview.docx',
    softwareVersion: 'v5.2.0',
    feature: '核心功能',
    microservice: 'product-service',
    status: 'CHUNKED',
    size: 2621440,
    uploadTime: new Date(Date.now() - 432000000),
    knowledgeBaseId: 'kb-4',
    chunks: [
      { id: 'chunk-5-1', content: '本产品提供全面的功能模块，包括用户管理、数据处理、报表分析等核心功能。通过高效的架构设计，确保系统的稳定性和可扩展性。', size: 980 },
      { id: 'chunk-5-2', content: '用户管理模块支持角色权限控制，可以根据不同用户的角色分配相应的功能权限。系统还提供了审计日志功能，记录关键操作，便于追踪和审查。', size: 1050 }
    ]
  }
]);

// 计算属性：当前知识库
const currentKnowledgeBase = computed(() => {
  if (!currentKnowledgeBaseId.value) return null;
  return knowledgeBases.value.find(kb => kb.id === currentKnowledgeBaseId.value);
});

// 计算属性：当前知识库的文件
const filteredFiles = computed(() => {
  if (!currentKnowledgeBaseId.value) return [];
  return files.value.filter(file => file.knowledgeBaseId === currentKnowledgeBaseId.value);
});

// 获取状态文本
const getStatusText = (status: string) => {
  switch (status) {
    case 'ACTIVE':
      return '激活';
    case 'INACTIVE':
      return '停用';
    case 'MAINTENANCE':
      return '维护中';
    case 'INIT':
      return '初始化';
    case 'UPLOADED':
      return '已上传';
    case 'CONVERTING':
      return '转换中';
    case 'CONVERTED':
      return '已转换';
    case 'CHUNKED':
      return '已分片';
    case 'VECTOR_STORED':
      return '向量存储';
    default:
      return status;
  }
};

// 获取状态样式
const getStatusClass = (status: string) => {
  switch (status) {
    case 'ACTIVE':
      return 'bg-green-100 text-green-800';
    case 'INACTIVE':
      return 'bg-gray-100 text-gray-800';
    case 'MAINTENANCE':
      return 'bg-yellow-100 text-yellow-800';
    case 'INIT':
      return 'bg-gray-100 text-gray-800';
    case 'UPLOADED':
      return 'bg-blue-100 text-blue-800';
    case 'CONVERTING':
      return 'bg-yellow-100 text-yellow-800';
    case 'CONVERTED':
      return 'bg-green-100 text-green-800';
    case 'CHUNKED':
      return 'bg-purple-100 text-purple-800';
    case 'VECTOR_STORED':
      return 'bg-indigo-100 text-indigo-800';
    default:
      return 'bg-gray-100 text-gray-800';
  }
};

// 格式化文件大小
const formatFileSize = (size: number) => {
  if (size < 1024) return size + ' B';
  if (size < 1048576) return (size / 1024).toFixed(1) + ' KB';
  return (size / 1048576).toFixed(1) + ' MB';
};

// 格式化日期
const formatDate = (date: Date) => {
  return new Date(date).toLocaleString('zh-CN');
};

// 切换知识库
const switchKnowledgeBase = (knowledgeBaseId: string) => {
  currentKnowledgeBaseId.value = knowledgeBaseId;
};

// 编辑知识库
const editKnowledgeBase = (kb: KnowledgeBase) => {
  editingKnowledgeBase.value = kb;
  newKnowledgeBase.value = {
    name: kb.name,
    description: kb.description,
    type: kb.type
  };
  showCreateKnowledgeBaseModal.value = true;
};

// 删除知识库
const deleteKnowledgeBase = (kb: KnowledgeBase) => {
  if (confirm(`确定要删除知识库 "${kb.name}" 吗？此操作将删除该知识库下的所有文件，此操作不可恢复。`)) {
    // 删除知识库
    knowledgeBases.value = knowledgeBases.value.filter(item => item.id !== kb.id);
    // 删除知识库中的文件
    files.value = files.value.filter(file => file.knowledgeBaseId !== kb.id);
    // 如果当前知识库是被删除的知识库，则切换到第一个知识库
    if (currentKnowledgeBaseId.value === kb.id) {
      currentKnowledgeBaseId.value = knowledgeBases.value.length > 0 ? knowledgeBases.value[0].id : null;
    }
  }
};

// 保存知识库
const saveKnowledgeBase = () => {
  if (editingKnowledgeBase.value) {
    // 更新现有知识库
    const index = knowledgeBases.value.findIndex(kb => kb.id === editingKnowledgeBase.value?.id);
    if (index !== -1) {
      knowledgeBases.value[index] = {
        ...knowledgeBases.value[index],
        ...newKnowledgeBase.value,
        updatedAt: new Date()
      };
    }
  } else {
    // 创建新知识库
    const newKB: KnowledgeBase = {
      id: `kb-${Date.now()}`,
      ...newKnowledgeBase.value,
      status: 'ACTIVE',
      fileCount: 0,
      chunkCount: 0,
      createdAt: new Date(),
      updatedAt: new Date()
    };
    knowledgeBases.value.push(newKB);
  }
  
  closeKnowledgeBaseModal();
};

// 关闭知识库模态框
const closeKnowledgeBaseModal = () => {
  showCreateKnowledgeBaseModal.value = false;
  editingKnowledgeBase.value = null;
  newKnowledgeBase.value = {
    name: '',
    description: '',
    type: 'technical'
  };
};

// 文件选择处理
const handleFileSelect = (event: Event) => {
  const target = event.target as HTMLInputElement;
  if (target.files && target.files.length > 0) {
    selectedFile.value = target.files[0];
  }
};

// 拖拽处理
const handleDragOver = (event: DragEvent) => {
  event.preventDefault();
};

const handleDrop = (event: DragEvent) => {
  event.preventDefault();
  if (event.dataTransfer && event.dataTransfer.files.length > 0) {
    selectedFile.value = event.dataTransfer.files[0];
  }
};

// 关闭上传模态框
const closeUploadModal = () => {
  showUploadModal.value = false;
  newFile.value = {
    title: '',
    softwareVersion: '',
    feature: '',
    microservice: ''
  };
  selectedFile.value = null;
  if (fileInputRef.value) {
    fileInputRef.value.value = '';
  }
};

// 关闭分片模态框
const closeChunkModal = () => {
  showChunkModal.value = false;
  selectedFileForChunk.value = null;
  chunkMode.value = 'fixed_length';
  chunkLength.value = 1000;
  childChunkLength.value = 200;
};

// 关闭分片详情模态框
const closeChunkDetailModal = () => {
  showChunkDetailModal.value = false;
  selectedFileForChunk.value = null;
  chunkDetails.value = {
    chunks: [],
    maxChunkSize: 0,
    minChunkSize: 0,
    avgChunkSize: 0
  };
};

// 关闭分片可视化模态框
const closeChunkVisualizationModal = () => {
  showChunkVisualizationModal.value = false;
  selectedFileForChunk.value = null;
  chunkDetails.value = {
    chunks: [],
    maxChunkSize: 0,
    minChunkSize: 0,
    avgChunkSize: 0
  };
};

// 上传文件
const uploadFile = () => {
  if (!selectedFile.value) {
    alert('请选择文件');
    return;
  }

  if (!currentKnowledgeBaseId.value) {
    alert('请先选择一个知识库');
    return;
  }

  const newFileItem: FileItem = {
    id: `file-${Date.now()}`,
    title: newFile.value.title,
    originalName: selectedFile.value.name,
    softwareVersion: newFile.value.softwareVersion,
    feature: newFile.value.feature,
    microservice: newFile.value.microservice,
    status: 'UPLOADED',
    size: selectedFile.value.size,
    uploadTime: new Date(),
    knowledgeBaseId: currentKnowledgeBaseId.value,
    chunks: []
  };

  files.value.push(newFileItem);
  
  // 更新知识库文件计数
  const kbIndex = knowledgeBases.value.findIndex(kb => kb.id === currentKnowledgeBaseId.value);
  if (kbIndex !== -1) {
    knowledgeBases.value[kbIndex].fileCount += 1;
  }

  closeUploadModal();
};

// 删除文件
const deleteFile = (file: FileItem) => {
  if (confirm(`确定要删除文件 "${file.title}" 吗？此操作不可恢复。`)) {
    const kbIndex = knowledgeBases.value.findIndex(kb => kb.id === file.knowledgeBaseId);
    if (kbIndex !== -1) {
      knowledgeBases.value[kbIndex].fileCount -= 1;
      // 还需要减少分片数量，这里简化处理
      if (file.status === 'CHUNKED' || file.status === 'VECTOR_STORED') {
        knowledgeBases.value[kbIndex].chunkCount -= file.chunks?.length ?? 0;
      }
    }
    
    files.value = files.value.filter(f => f.id !== file.id);
  }
};

// 下载文件
const downloadFile = (file: FileItem) => {
  // 创建模拟下载链接（这里只是演示，实际应用中需要从服务器获取文件）
  const blob = new Blob(['Simulated file content for ' + file.title], { type: 'application/octet-stream' });
  const url = URL.createObjectURL(blob);
  const a = document.createElement('a');
  a.href = url;
  a.download = file.originalName;
  document.body.appendChild(a);
  a.click();
  document.body.removeChild(a);
  URL.revokeObjectURL(url);
};

// 打开分片模态框
const openChunkModal = (file: FileItem) => {
  if (!canChunk(file)) return;
  
  selectedFileForChunk.value = file;
  showChunkModal.value = true;
};

// 查看分片
const viewChunks = (file: FileItem) => {
  if (!canViewChunks(file)) return;
  
  selectedFileForChunk.value = file;
  
  // 计算分片统计信息
  if (file.chunks && file.chunks.length > 0) {
    const sizes = file.chunks.map(c => c.size);
    const totalSize = sizes.reduce((sum, size) => sum + size, 0);
    
    chunkDetails.value = {
      chunks: file.chunks,
      maxChunkSize: Math.max(...sizes),
      minChunkSize: Math.min(...sizes),
      avgChunkSize: Math.round(totalSize / sizes.length)
    };
  } else {
    chunkDetails.value = {
      chunks: [],
      maxChunkSize: 0,
      minChunkSize: 0,
      avgChunkSize: 0
    };
  }
  
  showChunkDetailModal.value = true;
};

// 打开分片可视化
const openChunkVisualization = (file: FileItem) => {
  if (!canViewChunks(file)) return;
  
  selectedFileForChunk.value = file;
  
  // 计算分片统计信息
  if (file.chunks && file.chunks.length > 0) {
    const sizes = file.chunks.map(c => c.size);
    const totalSize = sizes.reduce((sum, size) => sum + size, 0);
    
    chunkDetails.value = {
      chunks: file.chunks,
      maxChunkSize: Math.max(...sizes),
      minChunkSize: Math.min(...sizes),
      avgChunkSize: Math.round(totalSize / sizes.length)
    };
  } else {
    chunkDetails.value = {
      chunks: [],
      maxChunkSize: 0,
      minChunkSize: 0,
      avgChunkSize: 0
    };
  }
  
  showChunkVisualizationModal.value = true;
};

// 执行分片
const performChunking = () => {
  if (!selectedFileForChunk.value) return;
  
  // 模拟分片处理
  setTimeout(() => {
    // 更新文件状态
    const fileIndex = files.value.findIndex(f => f.id === selectedFileForChunk.value?.id);
    if (fileIndex !== -1) {
      files.value[fileIndex].status = 'CHUNKED';
      
      // 生成模拟分片数据
      const mockChunks: Chunk[] = [];
      const contentBase = '这是分片内容的示例，包含有关该文档主题的关键信息。分片处理有助于提高检索效率和准确性。';
      
      for (let i = 0; i < 5; i++) {
        mockChunks.push({
          id: `chunk-${selectedFileForChunk.value?.id}-${i + 1}`,
          content: `${contentBase} 第 ${i + 1} 部分，用于演示分片效果。`,
          size: Math.floor(Math.random() * 500) + 500
        });
      }
      
      files.value[fileIndex].chunks = mockChunks;
      selectedFileForChunk.value = files.value[fileIndex];
      
      // 更新知识库分片计数
      const kbIndex = knowledgeBases.value.findIndex(kb => kb.id === files.value[fileIndex].knowledgeBaseId);
      if (kbIndex !== -1) {
        knowledgeBases.value[kbIndex].chunkCount += mockChunks.length;
      }
    }
    
    closeChunkModal();
  }, 1500);
};

// 同步知识库
const syncKnowledgeBase = () => {
  if (!currentKnowledgeBaseId.value) return;
  
  alert('开始同步知识库向量库...');
  // 这里可以添加实际的同步逻辑
  const kbIndex = knowledgeBases.value.findIndex(kb => kb.id === currentKnowledgeBaseId.value);
  if (kbIndex !== -1) {
    knowledgeBases.value[kbIndex].updatedAt = new Date();
  }
};

// 检查是否可以分片
const canChunk = (file: FileItem) => {
  return file.status === 'CONVERTED' || file.status === 'UPLOADED';
};

// 检查是否可以查看分片
const canViewChunks = (file: FileItem) => {
  return file.status === 'CHUNKED' || file.status === 'VECTOR_STORED';
};

// 查看分片详情
const viewChunkDetail = (chunk: Chunk) => {
  alert(`分片内容:\n\n${chunk.content}`);
};

// 复制到剪贴板
const copyToClipboard = async (text: string) => {
  try {
    await navigator.clipboard.writeText(text);
    alert('已复制到剪贴板');
  } catch (err) {
    console.error('复制失败:', err);
    alert('复制失败');
  }
};

// 计算高亮内容
const highlightedContent = computed(() => {
  if (!selectedFileForChunk.value || !selectedFileForChunk.value.chunks || selectedFileForChunk.value.chunks.length === 0) {
    return '';
  }

  // 合并所有分片内容作为原始文档
  let originalContent = '';
  selectedFileForChunk.value.chunks.forEach(chunk => {
    originalContent += chunk.content + ' ';
  });

  // 创建一个数组来标记每个字符的分片归属
  const charMap: number[] = new Array(originalContent.length).fill(-1);
  const overlapMap: boolean[] = new Array(originalContent.length).fill(false);

  // 为每个分片标记其在原文档中的位置
  let pos = 0;
  selectedFileForChunk.value.chunks.forEach((chunk, chunkIndex) => {
    const chunkText = chunk.content;
    const chunkStart = originalContent.indexOf(chunkText, pos);
    
    if (chunkStart !== -1) {
      for (let i = chunkStart; i < chunkStart + chunkText.length; i++) {
        if (charMap[i] !== -1) {
          // 发现重叠部分
          overlapMap[i] = true;
        }
        charMap[i] = chunkIndex;
      }
      pos = chunkStart + chunkText.length;
    }
  });

  // 构建高亮的HTML
  let result = '';
  let currentChunkIndex = -1;
  
  for (let i = 0; i < originalContent.length; i++) {
    const char = originalContent[i];
    
    if (charMap[i] !== currentChunkIndex) {
      // 关闭之前的标签
      if (currentChunkIndex !== -1) {
        result += '</span>';
      }
      
      // 开始新的标签
      if (charMap[i] !== -1) {
        if (overlapMap[i]) {
          // 重叠部分使用特殊颜色
          result += `<span class="bg-yellow-300">${char}`;
        } else {
          // 不同分片使用不同颜色
          const colors = ['bg-blue-100', 'bg-green-100', 'bg-purple-100', 'bg-pink-100', 'bg-indigo-100', 'bg-red-100', 'bg-teal-100'];
          const color = colors[charMap[i] % colors.length];
          result += `<span class="${color}">${char}`;
        }
      } else {
        result += char;
      }
      
      currentChunkIndex = charMap[i];
    } else {
      result += char;
    }
  }
  
  // 关闭最后一个标签
  if (currentChunkIndex !== -1) {
    result += '</span>';
  }
  
  return result;
});
</script>

<style scoped>
/* 自定义按钮圆角样式 */
.rounded-button {
  border-radius: 8px;
}
</style>

